// The canonical address. The bare domain is the main one; www serves the same pages.
export const SITE_URL = "https://riolargo.co.uk";

export const ROUTES = ["/", "/shop", "/about", "/shipping", "/contact", "/privacy"] as const;
